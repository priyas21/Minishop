<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "Minishop", root_path, id: "logo" %>
    </div>
    <ul class="nav nav-pills navbar-right">
      <li><%= link_to "Home", root_path %></li>
      <% if logged_in? %>
        <li class="dropdown">

        <% if current_user.admin? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
         Admin Account <b class="caret"></b></a>
         <ul class="dropdown-menu">
            <li><%= link_to "All Users", admin_addresses_path %></li>
            <li><%= link_to "My Profile", user_address_path(current_user, current_address) %></li>
            <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", logout_path, method: :delete %></li>
          </ul>

        <% else %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.first_name %>
          's Account<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "My Profile", user_address_path(current_user, current_address) %></li>
            <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
            <li><%= link_to "Edit Address", edit_user_address_path(current_user, current_address) %></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", logout_path, method: :delete %></li>
          </ul>
        <% end %>

      <% else %>
        <li><%= link_to "Log in", login_path %> </li>
      <% end %>
    </ul>
  </div>
</nav>
